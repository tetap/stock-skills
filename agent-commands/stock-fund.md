# 资金面分析

用户在本条消息 `/stock-fund` **后面**的文字是股票名称或代码。

## 你要做的

1. 读取并遵循 **stock-capital-flow** skill
2. 解析标的 → 拉取近 20 日 `get_stock_fund_flow`、必要时 `get_fund_flow_rank` 与 `get_market_fund_flow`
3. 说明主力流向趋势、与股价是否背离、市场/板块背景
4. 末尾加免责声明
