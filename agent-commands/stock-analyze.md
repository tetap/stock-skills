# 全面分析（/stock 别名）

等价于 **`/stock 分析 {标的}`** → **stock-main** 全量拉数 + **analysis-report.md** + **review-protocol**。

## 你要做的

1. **stock-main** + **stock-analysis-orchestrator**
2. 按 analysis-report.md **拉齐全部工具（≥20 次）**，含 `get_quant_technical`、新闻/情绪
3. 调用 `get_review_protocol(flow=B)`，过 R2~R6 门禁
4. 输出 **7 节终稿**（§1~§6 报告 + **§7 审核纪要**），**不用顾问角色**
